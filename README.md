index.html: The frontend interface providing a file dropzone, theme toggles, scenario comparison tables, and interactive dashboards.
script.js: Contains the core application logic, including the heuristic solver, CSV parsing (using PapaParse), self-check validators, and inline SVG network rendering.
style.css: Provides the global styling, responsive layout variables, and seamless light and dark color schemes.
